import HomeActionTypes from "Containers/Home/types"

const INITIAL_STATE = {}

const homeReducer = (state = INITIAL_STATE, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}

export default homeReducer
